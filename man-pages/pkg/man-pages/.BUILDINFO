builddir = /sources/man-pages
pkgbuild_sha256sum = 
buildenv = !distcc
buildenv = color
buildenv = !ccache
buildenv = check
buildenv = !sign
options = strip
options = docs
options = libtool
options = staticlibs
options = emptydirs
options = zipman
options = purge
options = !upx
options = !optipng
options = !debug
installed = nano-7.2-1
